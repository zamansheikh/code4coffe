#include<stdio.h>
#include<cornio.h>

int main()
{
    int arr[50],i,s;
    printf("Enter the size of the array: ");
    scanf(%d, &s);
    printf("Enter the elements of the array: ");
    for(int i=0; i<size; i++)
    {
        scanf("%d, &i++");

    }
    printf("/n All negetive elements in array are: "):
    for(i=0; i<s; i++)
    {
        if(arr[i]<0)
        {
            printf("%d", arr[i]);
        }
    }
}
